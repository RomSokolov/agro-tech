# PostgreSQL connection string.
# Local development and production both use a hosted Postgres database
# (e.g. Neon). Copy this file to .env and set your own connection string.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
