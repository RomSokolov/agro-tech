# SQLite for local development.
# For production (e.g. Vercel) switch the datasource provider to "postgresql"
# and point this at a hosted Postgres connection string.
DATABASE_URL="file:./prisma/dev.db"
